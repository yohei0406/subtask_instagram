<% provide(:title, @title ) %>
<% if @users.any? %>
  <ul class="users follow">
  <% @users.each do |user| %>
  　　<li><%= link_to user.username, user %></li>
  <% end %>
  </ul>
<% else %>
  <h3>フォロワー・フォロー中のユーザーはいません</h3>
<% end %>